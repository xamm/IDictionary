language: node_js

node_js:
  - 'stable'

sudo: false

env:
  - workerCount=3 timeout=600000

matrix:
  fast_finish: true

install:
  - yarn

script:
    - yarn test
cache:
  yarn: true
  directories:
    - node_modules