sudo: false

node_js:
  - "stable"
os:
  - linux

before_install:
  - npm -g typescript

install:
  - yarn
  - yarn postpublish

script:
  - yarn test

cache:
  yarn: true
  directories:
    - node_modules
